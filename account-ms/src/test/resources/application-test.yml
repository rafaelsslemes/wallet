spring:
  profiles:
    active: test
  application:
    name: account-ms
  datasource:
    url: jdbc:h2:mem:account-ms
    username: sa
    password:
    driverClassName: org.h2.Driver
    jpa:
      # database: H2
      show-sql: true
      hibernate.ddl-auto: update
      database-platform: org.hibernate.dialect.H2Dialect

